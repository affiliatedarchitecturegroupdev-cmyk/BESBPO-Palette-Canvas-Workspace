/**
 * Contact points for the workspace surface.
 *
 * Source: the specification package's `contact-details.md`. That file is
 * explicit that these are the real, existing contact points and that no
 * workspace-specific phone number exists — the earlier UK subdomain work
 * deliberately dropped phone numbers rather than inventing one. Do not add a
 * phone number here without confirming a real one first.
 */
export const SUPPORT_EMAIL = 'agencies@palettecanvas.work';

/** Corporate-domain address used for group-level contact. */
export const CORPORATE_EMAIL = 'info@palettecanvas.besbpo.co.za';